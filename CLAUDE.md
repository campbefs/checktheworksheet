# CLAUDE.md — child-support-reform-public

Guidance for Claude Code working in this repository.

## Project memory

Durable, project-scoped memory lives in `context/MEMORY.md` and is imported below, so it
loads into every session in this folder. Session logs are the dated files in `context/` —
different thing. Before `/clear`, do a promote pass into that file.

@context/MEMORY.md
